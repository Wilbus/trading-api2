namespace schwabMarketData;

enum ExchangeName : byte
{
    IND = 0,
    ASE,
    NYS,
    NAS,
    NAP,
    PAC,
    OPR,
    BATS
}

enum OptionStrategy : byte
{
    SINGLE = 0,
    ANALYTICAL,
    COVERED,
    VERTICAL,
    CALENDAR,
    STRANGLE,
    STRADDLE,
    BUTTERFLY,
    CONDOR,
    DIAGONAL,
    COLLAR,
    ROLL
}

enum PutCall : byte
{
    PUT = 0,
    CALL
}

enum ExpirationType : byte
{
    M = 0,
    Q,
    S,
    W
}

enum SettlementType : byte
{
    A = 0,
    P
}